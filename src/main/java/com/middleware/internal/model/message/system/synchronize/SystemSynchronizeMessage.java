package com.middleware.internal.model.message.system.synchronize;

import com.middleware.internal.model.message.system.AbstractSystemMessage;

public class SystemSynchronizeMessage extends AbstractSystemMessage {
    // Attributes

    // Constructors
    public SystemSynchronizeMessage() {
        super(null);
    }

    // Getters & Setters

    // Methods
}
