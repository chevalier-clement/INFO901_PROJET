package com.middleware.internal.model.message.client.asynchronous;

import com.middleware.internal.model.message.client.AbstractClientMessage;

public class AbstractAsynchronousClientMessage extends AbstractClientMessage {
    // Attributes

    // Constructors
    public AbstractAsynchronousClientMessage(int from, Object o, int lamportValue) {
        super(from, o, lamportValue);
    }

    // Getters & Setters

    // Methods
}
